# 光流、深度、物理粒子与高斯协同更新：小白版

本文说明下一阶段准备加入的视觉状态更新算法。核心目标是：

> 先用光流找到组织在下一帧往哪里移动，再用双目深度确定它在三维空间移动了多少；随后更新物理粒子的位置和速度，最后让绑定在组织表面的高斯自动跟随。

参考论文是 TrackerSplat：

- 论文：https://arxiv.org/abs/2604.02586
- HTML：https://arxiv.org/html/2604.02586v1
- 官方代码：https://github.com/yindaheng98/TrackerSplat

本文只描述改进方案，不表示算法已经实现或指标已经提升。实际结论必须由重建 7-1、未来预测 80-20 和完整五项指标评估给出。

---

## 1. 现在系统里有什么

当前 SUPER 组织模型主要有三层：

| 层 | 数量 | 用途 |
|---|---:|---|
| 物理粒子 | 1,465 | 保存组织的位置、速度、质量和材料状态 |
| 四面体 | 5,502 | 维持距离、形状和体积等物理约束 |
| 组织 Gaussian | 26,754 | 根据相机视角渲染组织外观 |

组织 Gaussian 不是自由漂浮的点。每个 Gaussian 都绑定在高分辨率视觉三角面上，而视觉三角面又绑定到物理粒子。因此当前正确的数据关系是：

```text
物理粒子移动
    ↓
视觉三角面变形
    ↓
Gaussian 中心、旋转、大小变化
    ↓
重新渲染图像
```

当前代码已经能够做到：

1. RGB 视觉残差修改物理粒子位置 `particle_q`；
2. 接受视觉修正后同步修改速度 `particle_qd`；
3. 根据视觉三角面形变更新 Gaussian 的中心、旋转和尺度；
4. 对四面体翻转、低体积、穿透和固定区域进行安全检查；
5. 在未来预测测试段禁止使用未来 RGB 更新。

下一阶段不是推翻这些功能，而是在 RGB residual 前面增加一个更强的“大位移视觉观测器”。

---

## 2. 为什么现在的 RGB residual 效果不够明显

当前视觉 residual 的基本过程是：

```text
渲染当前组织
    ↓
和真实 RGB 图像比较
    ↓
根据图像 loss 的梯度移动物理粒子
```

它适合处理已经比较接近的情况，但对大偏差存在三个问题。

### 2.1 RGB 梯度通常是局部的

如果预测组织只偏了几像素，图像梯度通常可以告诉优化器往哪里移动。但如果组织已经偏了几十像素，预测区域和真实区域可能没有重叠，梯度就不能提供正确方向。

这时常见现象是：

- 真实组织明明在左边，预测组织在右边，但 residual 没有明显往左移动；
- 优化器通过轻微改变局部颜色或轮廓降低 loss，而不是纠正整体位置；
- 更新被固定的单步位移上限限制，很多帧后仍追不上真实运动。

TrackerSplat 也将“大位移超出局部光度梯度的有效范围”作为主要问题之一，所以它先用点跟踪完成运动定位，再进行光度优化。

### 2.2 二维 RGB 不容易判断三维方向

左目图像中同样的二维移动，既可能来自组织在图像平面内移动，也可能来自组织沿相机深度方向运动。只有 RGB loss 时，优化器需要通过渲染梯度间接猜测三维方向。

双目深度可以直接提供更明确的三维信息。

### 2.3 位置被修正后，错误速度可能把它拉回去

如果只改位置、不改速度，下一次物理积分仍会沿旧速度前进。因此当前系统已经增加：

\[
v^+ = v^- + \beta\frac{\delta q}{\Delta t}
\]

新的光流—深度观测也必须同时更新位置和速度。

---

## 3. 最重要的改进方向

新的完整流程建议为：

```text
XPBD 预测下一帧物理状态
          ↓
光流/点跟踪得到二维移动方向
          ↓
双目深度把二维目标恢复成三维运动观测
          ↓
通过三角面—粒子绑定反求物理粒子修正量
          ↓
执行四面体、体积、固定点和穿透安全投影
          ↓
更新物理粒子位置和速度
          ↓
现有 mode-2 绑定自动更新 Gaussian
          ↓
使用小范围 RGB residual 做最后精修
          ↓
使用接受后的物理形变作为刚度更新证据
```

这里有一个非常重要的原则：

> 光流和深度负责找对运动方向与大致距离，RGB residual 负责修正最后的小误差。

不要再要求 RGB residual 独自完成几十像素的大范围搜索。

---

## 4. 第一步：用光流找到下一帧二维位置

假设物理预测得到一个表面点的三维位置：

\[
X_{i,t}^{-}
\]

把它投影到左目或右目图像：

\[
u_{i,t}^{c}=\pi_c(X_{i,t}^{-}),\qquad c\in\{L,R\}
\]

其中：

- \(X\) 是三维位置；
- \(u\) 是二维像素位置；
- \(c\) 表示左相机或右相机；
- \(\pi_c\) 表示相机投影。

时序光流给出这个像素从当前帧到下一帧的移动量：

\[
F_{t\rightarrow t+1}^{c}(u_{i,t}^{c})
\]

因此下一帧二维目标位置是：

\[
\hat u_{i,t+1}^{c}
=
u_{i,t}^{c}
+
F_{t\rightarrow t+1}^{c}(u_{i,t}^{c})
\]

小白理解就是：

```text
当前像素位置 + 光流箭头 = 下一帧目标像素位置
```

需要注意：当前项目里的 RAFT-Stereo 主要计算同一时刻左右相机之间的视差，不等于时间方向的 optical flow。下一阶段需要单独增加时间光流或点跟踪模型，例如 RAFT temporal flow、DOT 或 CoTracker。

第一版建议离线预计算光流，先证明评估指标能够提升，再优化到在线运行。

---

## 5. 第二步：用双目深度得到三维目标

只知道二维像素目标还不够，因为物理粒子在三维空间中运动。可以在下一帧目标像素处读取深度：

\[
D_{t+1}^{c}(\hat u_{i,t+1}^{c})
\]

然后利用相机内参和外参反投影到世界坐标：

\[
\hat X_{i,t+1}^{c}
=
T_{Wc}
\left[
D_{t+1}^{c}(\hat u_{i,t+1}^{c})
K_c^{-1}
\tilde{\hat u}_{i,t+1}^{c}
\right]
\]

可以把它理解成：

```text
目标像素告诉我们“射线方向”
深度告诉我们“沿射线走多远”
两者组合得到下一帧三维位置
```

### 5.1 不应完全相信一个深度像素

手术图像中存在反光、低纹理、器械遮挡和组织边界。深度可能在这些位置出错，所以必须为每个三维观测计算置信度。

建议置信度包含：

\[
w_i=
w_{\mathrm{flow}}
w_{\mathrm{stereo}}
w_{\mathrm{depth}}
w_{\mathrm{mask}}
w_{\mathrm{visibility}}
w_{\mathrm{occlusion}}
\]

实际实现时使用乘积或加权组合。主要检查包括：

- 时序光流前向、后向是否一致；
- FoundationStereo 左右一致性是否通过；
- FoundationStereo 与 RAFT-Stereo 是否严重矛盾；
- 像素是否位于组织 mask 内；
- 像素是否被器械或膨胀器械 mask 遮挡；
- 当前物理表面在该像素是否真正可见；
- 深度是否位于合理范围、是否落在深度突变边界。

### 5.2 三维增量比绝对深度更稳健

双目深度可能有逐帧整体偏差。第一版建议主要使用两帧反投影位置的差值：

\[
\Delta\hat X_i
=
\operatorname{BackProj}(D_{t+1},\hat u_{t+1})
-
\operatorname{BackProj}(D_t,u_t)
\]

这表示观察到的三维运动量。然后要求模型表面也产生类似运动：

\[
Y_i(q_{t+1})-Y_i(q_t)
\approx
\Delta\hat X_i
\]

绝对三维位置仍可作为较弱的防漂移约束，但不应在开始阶段占主导。

---

## 6. 第三步：从表面观测反求物理粒子

摄像机只能看到组织表面，不能直接看到组织内部的四面体粒子。因此不能给所有 1,465 个粒子分别读取深度。

正确方法是：

1. 对可见视觉顶点或 Gaussian 中心建立三维目标；
2. 根据现有绑定关系，计算哪些物理粒子控制这些表面点；
3. 反求一个整体一致的物理粒子修正；
4. 让内部粒子通过四面体约束自然跟随。

设表面点由物理粒子状态产生：

\[
Y_j(q)=\operatorname{Skinning}_j(q)
\]

可以求下面的目标：

\[
\begin{aligned}
\delta q^*=\arg\min_{\delta q}\quad
&\sum_j w_j
\rho\left(
\|Y_j(q^-+\delta q)-\hat X_j\|^2
\right)\\
&+\lambda_{2D}\sum_{j,c}w_j^c
\rho\left(
\|\pi_c(Y_j(q^-+\delta q))-\hat u_j^c\|^2
\right)\\
&+\lambda_eE_{\mathrm{edge}}
+\lambda_vE_{\mathrm{volume}}
+\lambda_sE_{\mathrm{shape}}
+\lambda_tE_{\mathrm{temporal}}
+\lambda_m\|\delta q\|^2
\end{aligned}
\]

各项的简单含义：

| 项 | 作用 |
|---|---|
| 三维观测误差 | 让表面走向光流和深度给出的三维位置 |
| 二维重投影误差 | 确保左右相机看到的位置正确 |
| edge | 防止相邻粒子被随意拉开 |
| volume | 防止四面体体积塌陷或翻转 |
| shape | 防止局部形状出现不合理扭曲 |
| temporal | 防止相邻时间更新抖动 |
| magnitude | 防止低置信度观测造成巨大跳跃 |

固定粒子、anchor 和正在被器械控制的粒子必须满足：

\[
\delta q_i=0
\]

当前 RGB residual 已经具备很多相同的物理正则和安全检查，所以新模块可以复用，而不是重新写一整套物理保护。

---

## 7. 第四步：同时更新位置和速度

建议使用置信度控制的 alpha-beta 更新：

\[
q^+=q^-+\alpha_i\delta q_i^*
\]

\[
v^+=v^-+\beta_i\frac{\delta q_i^*}{\Delta t}
\]

其中高置信度粒子使用较大的 \(\alpha_i,\beta_i\)，低置信度粒子使用较小的值。

简单理解：

- 改 `q`：把组织拉到观察到的位置；
- 改 `v`：告诉物理系统组织正在朝这个方向运动；
- 不改 `v`：下一帧可能又沿旧速度跑回错误方向。

位置和速度更新后，还要执行一次 XPBD 安全投影，保证：

- 不新增翻转四面体；
- 不增加低体积四面体；
- volume 在允许范围；
- 不明显增加器械穿透；
- anchor 和固定区域不移动；
- 非有限数值立即回退。

---

## 8. 第五步：让 Gaussian 自动跟随物理粒子

不建议光流先独立移动 Gaussian，再另外修改物理粒子。这样会产生两份互相矛盾的位置状态：

```text
Gaussian 认为组织在 A
物理粒子认为组织在 B
```

下一次绑定更新时，Gaussian 又会被物理粒子拉回去。

当前 mode-2 绑定已经提供正确做法。物理粒子更新后，视觉三角面的三个顶点变成：

\[
v_{g,1}',\quad v_{g,2}',\quad v_{g,3}'
\]

Gaussian 中心为：

\[
\mu_g'=rac{v_{g,1}'+v_{g,2}'+v_{g,3}'}{3}
\]

三角面形变矩阵为：

\[
F_g=
[e_1',e_2',n']
[e_1^0,e_2^0,n^0]^{-1}
\]

Gaussian 协方差更新为：

\[
\Sigma_g'=F_g\Sigma_g^0F_g^T
\]

因此：

- 三角面平移，Gaussian 中心跟随；
- 三角面旋转，Gaussian 朝向跟随；
- 三角面拉伸或剪切，Gaussian 大小和形状跟随。

新算法只要接受物理粒子状态，然后调用现有 Gaussian transform 更新即可。

---

## 9. 与 TrackerSplat 的关系

### 9.1 直接借鉴的部分

TrackerSplat 的重要思想是：

> 光度梯度适合小范围精修，点跟踪更适合先解决跨帧大位移。

论文根据像素轨迹估计每个 Gaussian 的二维仿射运动。它使用 Gaussian 对像素的 alpha blending 贡献作为权重：

\[
w_{gp}=\alpha_{gp}T_p
\]

然后估计：

\[
\min_{A_g,b_g}
\sum_p w_{gp}
\|A_gp+b_g-(p+F(p))\|^2
\]

二维 Gaussian 中心变为：

\[
\mu_{g,2D}'=A_g\mu_{g,2D}+b_g
\]

当前项目可以借鉴：

1. 使用 alpha/transmittance 判断哪个像素真正属于哪个 Gaussian；
2. 用多个像素的运动拟合一个稳定的 Gaussian 二维运动；
3. 少于三个有效像素或拟合矩阵病态时拒绝该观测；
4. 对不可靠运动做邻域中值滤波；
5. 完成大位移定位后，再使用 RGB loss 精修。

### 9.2 不能直接照搬的部分

TrackerSplat 主要优化变量是 Gaussian，而当前项目还需要保持组织物理一致性。因此必须做下面的改造：

| TrackerSplat | 当前项目的改造 |
|---|---|
| 直接更新 Gaussian 运动 | 先把跟踪结果转成物理粒子观测 |
| 多视图三角化 Gaussian 中心 | 主要使用 SUPER 双目深度和左右重投影 |
| 多视图估计 Gaussian 3D covariance | 继续使用真实视觉三角面形变更新 covariance |
| 欧氏空间 KNN 运动滤波 | 使用三角网格/四面体拓扑邻接，避免折叠表面串层 |
| Gaussian 光度优化保证外观 | 还必须通过 tet、volume、penetration、anchor 等物理安全门 |
| 每帧可以读取目标图像 | 正式 7-1 和 80-20 评估必须严格防止测试帧泄漏 |

所以这不是简单复现 TrackerSplat，而是将它的“tracking-first”思想变成一个物理状态观测器。

### 9.3 推荐保留的分工

```text
TrackerSplat思想：点跟踪负责跨帧大运动
                  ↓
本项目新增：深度把二维运动恢复成三维观测
                  ↓
本项目核心：物理绑定和XPBD保证组织一致性
                  ↓
现有能力：动态Gaussian中心/旋转/尺度更新
                  ↓
现有RGB residual：完成最后的小范围图像精修
```

---

## 10. 为什么不能只使用光流

只使用光流可以明显改善二维轨迹，但可能出现：

- 左目二维位置正确，三维深度错误；
- 表面沿错误的相机射线移动；
- 2D tracking error 下降，但 3D tracking error 上升；
- Gaussian 重投影看似正确，物理组织厚度和接触位置不正确。

因此需要光流和深度共同工作：

| 信息 | 主要回答的问题 |
|---|---|
| 光流 | 下一帧在图像中往哪个方向移动 |
| 双目深度 | 下一帧在三维空间中位于多远 |
| 物理约束 | 哪些三维形变是组织可以产生的 |
| RGB residual | 渲染轮廓、纹理和颜色还有哪些小误差 |

---

## 11. 两级更新比单一大步更新更安全

建议把状态更新分成两级。

### 第一级：光流—深度粗校正

- 解决几十像素的大偏差；
- 置信度高的区域允许比当前 RGB residual 更大的修正；
- 建议从 `0.25–2.0 mm` 的置信度自适应范围开始验证；
- 先进行局部刚体或仿射校正，再处理非刚性细节；
- 所有结果必须经过硬物理门。

### 第二级：RGB residual 精修

- 保留较小的单次位移上限；
- 修正 flow/depth 噪声和亚像素轮廓误差；
- 优化左右相机的最终渲染一致性；
- 不再负责从几十像素外寻找正确位置。

如果第一阶段观测置信度不足，应退回物理预测，而不是让 RGB residual 无限制搜索。

---

## 12. 对在线刚度更新的帮助

当前刚度更新依赖视觉接受状态和边应变证据。如果视觉 residual 没有沿真实运动方向修正，刚度更新看到的材料证据也会错误。

新的 flow-depth observer 可以提供更明确的状态创新量：

\[
\delta q_{\mathrm{obs}}=q^+-q^-
\]

然后比较：

- 物理预测边应变；
- 光流—深度接受状态的边应变；
- 哪些区域反复需要同方向修正；
- distance 和 shape 误差分别怎样变化。

但建议先冻结刚度，单独证明 flow-depth 状态更新有效，再重新开启刚度优化。否则状态估计和材料估计同时改变，很难定位性能变化原因。

---

## 13. 必须严格遵守的评估因果性

### 13.1 重建 7-1

测试帧不能使用该帧的：

- RGB residual；
- 时序光流；
- 点跟踪目标；
- 深度图；
- 由该帧观测计算的刚度更新。

特别注意：从训练帧 \(t\) 计算到测试帧 \(t+1\) 的光流，同样读取了测试帧图像，因此也属于测试信息泄漏。

### 13.2 未来预测 80-20

允许在训练段 `0..1151` 使用：

- RGB；
- 光流；
- 双目深度；
- 状态修正；
- 在线刚度更新。

从 `1152` 开始必须：

- 不读取未来 RGB；
- 不读取未来光流；
- 不读取未来深度；
- 不进行视觉状态修正；
- 不继续估计材料；
- 只使用训练段最终状态和刚度开放环预测未来 20%。

---

## 14. 推荐的实施步骤

### 阶段 A：只生成观测，不修改物理状态

新增离线脚本，保存每帧：

- 左右目时序光流；
- 光流前后向误差；
- 光流目标像素；
- FoundationStereo 深度；
- RAFT-Stereo 一致性；
- 三维运动观测；
- 每个观测的最终置信度；
- 被拒绝的原因。

先在人工标注的 10 个点上检查二维方向和三维位移是否正确。

### 阶段 B：只做 flow-depth 物理状态更新

暂时关闭：

- RGB residual；
- 在线刚度更新。

只比较：

```text
Pure PBD
PBD + flow-depth observer
```

如果二维和三维误差不能同时改善，先修观测，不进入下一阶段。

### 阶段 C：加入 RGB 精修

比较：

```text
PBD
PBD + 原 RGB residual
PBD + flow-depth observer
PBD + flow-depth observer + RGB residual
```

目标是 flow-depth 负责大运动、RGB residual 负责小误差，组合方法应明显超过单独 RGB residual。

### 阶段 D：重新加入在线刚度

最终比较：

```text
PBD
PBD + RGB residual
PBD + flow-depth
PBD + flow-depth + RGB residual
PBD + flow-depth + RGB residual + 在线刚度
```

在线刚度必须在未来预测指标上体现额外优势，不能只增加提交次数。

---

## 15. 正式评估指标

每组都需要报告：

### 轨迹指标

- 2D 点跟踪 mean error；
- 2D 点跟踪 RMSE；
- 3D 点跟踪 mean error；
- 3D 点跟踪 RMSE。

### 渲染指标

- PSNR，越高越好；
- SSIM，越高越好；
- LPIPS，越低越好。

### 状态观测诊断

- 光流有效覆盖率；
- 光流前后向一致率；
- 有效深度覆盖率；
- 左右重投影误差；
- 修正方向与人工轨迹方向一致率；
- 每帧最大和 RMS 位置修正；
- 每帧最大和 RMS 速度修正；
- 被 mask、遮挡、深度和物理门拒绝的数量。

### 物理安全指标

- 新增翻转四面体数量必须为 0；
- 低体积四面体不能明显增加；
- anchor 误差不能增加；
- penetration 不能明显恶化；
- future 测试段视觉更新次数必须为 0。

---

## 16. 建议新增的代码结构

建议新增一个独立模块，而不是继续扩大现有 RGB residual 文件：

```text
src/embodied_gaussians/physics_simulator/
    flow_depth_particle_observer.py
```

它主要负责：

1. 读取缓存的 temporal flow 和 depth；
2. 投影当前可见表面点；
3. 采样二维目标和置信度；
4. 生成三维位置/位移观测；
5. 通过现有绑定求解 `delta_q`；
6. 生成位置和速度候选；
7. 执行物理安全检查；
8. 返回接受结果和完整诊断。

建议再新增离线数据脚本：

```text
scripts/generate_super_temporal_flow_depth_observations.py
```

正式运行顺序可以接在当前 XPBD 预测之后、RGB residual 之前：

```python
physics_prediction()
flow_depth_observer.correct_state()
sim.update_gaussian_transforms()
rgb_residual.refine_state()
online_stiffness.observe_accepted_state()
```

---

## 17. 预期优势和主要风险

### 预期优势

- 可以直接处理当前 RGB residual 难以解决的几十像素偏差；
- 同时使用二维方向和三维深度，2D/3D 指标更容易一起改善；
- 位置和速度同步更新，修正不容易被下一步物理状态拉回；
- Gaussian 继续服从真实三角面，不破坏物理—视觉一致性；
- 更准确的状态创新可以为刚度优化提供更可靠的边应变证据。

### 主要风险

- 低纹理和反光组织会产生光流抖动；
- 器械遮挡可能把组织点跟踪到器械上；
- 深度边界可能出现跳变；
- 左右相机不同步会制造错误三维速度；
- 直接相信绝对深度可能把整个组织推离当前坐标系；
- 普通欧氏 KNN 可能在折叠组织上错误连接两层表面；
- 如果测试段仍使用 flow/depth，会产生严重评估泄漏。

对应解决方式是置信度融合、增量深度、拓扑邻域滤波、物理硬门和严格因果边界，而不是简单增大所有更新步长。

---

## 18. 最终总结

这次改进不是“换一种 loss”，而是改变视觉更新的职责分工：

```text
旧方案：
RGB residual 同时负责寻找大运动方向、确定三维位置和精修渲染

新方案：
光流负责二维大运动方向
双目深度负责三维距离
物理求解负责组织一致性
Gaussian 绑定负责渲染几何同步
RGB residual 负责最后的小误差精修
在线刚度负责改善未来开放环预测
```

与 TrackerSplat 的核心关系是借鉴其“tracking-first、photometric-refinement-second”的思路；与其最大的区别是，本项目不能让 Gaussian 独立运动，而必须把跟踪结果先变成物理粒子状态，再由三角面绑定驱动 Gaussian。

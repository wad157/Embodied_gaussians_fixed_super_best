# SUPER 评估：10 个组织点人工标注指南

## 1. 这一步到底在做什么

这里要制作一份**只用于考试、不参与训练**的组织轨迹真值。每个点代表组织表面上一个始终保持
同一身份的纹理标志，例如血管分叉、斑点交叉或清楚的小凹陷。

这不是只在第一帧点击 10 下就结束。完整轨迹中，每个采样帧的每个点都必须被人确认成三种
状态之一：

- `visible`：能看见，并有准确的二维像素坐标；
- `occluded`：暂时被夹爪或组织挡住，仍保留其遮挡后的估计位置；
- `out_of_view`：已经离开画面，没有二维坐标。

自动光流只提供下一帧的建议位置。只有你看过整帧并按下 `ENTER` 后，这一帧才会写成
`human_verified=true`，因此不会把自动跟踪结果冒充人工真值。

## 2. 为什么采用这种方案

[Tracking Everything in Robotic-Assisted Surgery](https://arxiv.org/abs/2409.19821) 对每段约
50--70 帧的视频逐帧人工标点，组织点优先选择血管交叉等有纹理的位置，并单独标记遮挡和离开
画面。它说明了外科视频真值最重要的两件事：**点的身份必须跨帧不变，遮挡不能当成跟踪错误
或随手换成另一个点**。

[PhysTwin](https://arxiv.org/abs/2503.17973) 每个视频使用 9 个手工真值点，并使用 TAPIR 式
半自动工具降低逐帧标注成本。其训练监督则来自 CoTracker3 后再用深度反投影到三维；手工点只
负责独立评测。这正是本项目需要遵守的“训练数据和考题分开”。

[Embodied Gaussians](https://arxiv.org/abs/2406.10788) 在初始时刻找到离查询点最近的 Gaussian，
随后跟踪这个 Gaussian 所绑定的物理状态，最后计算平均二维和三维轨迹误差。本项目后续会把
手工点绑定到组织表面三角形/物理节点，采用同样的持久身份思路。

仓库已有的 32 条 LK tracklet 曾用于材料标定损失，不能再作为独立测试真值；否则相当于把
练习题重新拿来考试。新的 10 点标注器不读取这些 tracklet，也不导入仿真器、视觉 residual
或在线刚度模块。

## 3. 本项目的固定标注协议

默认协议为：

| 项目 | 当前设置 |
|---|---:|
| 点数 | 10 个组织点 |
| 图像 | rectified stereo-left，`1920×1080` |
| 时间范围 | frame `0..1439` |
| 常规间隔 | 每 10 帧人工验收一次 |
| 总验收帧 | 149 帧 |
| 额外必标帧 | 接触、闭合、牵拉、最大回撤、释放、回弹 |
| 未来预测分界 | train `0..1151`，test `1152..1439` |
| 3D 不可用帧 | 自动跳过 8 个双目时间差超过 20 ms 的帧 |

`1151` 和 `1152` 被显式加入采样表，避免 80/20 分界处没有真值。这个协议最多包含
`149×10=1490` 个状态判断，但通常只有首帧需要点击 10 次；后面主要是检查建议、修正错误和
按 `ENTER`。

## 4. 第一帧怎样挑 10 个点

请严格按编号顺序点击：

- `T01--T04`：夹取附近的纹理点，负责测量直接受力区域；
- `T05--T07`：中距离纹理点，负责测量形变向外传播；
- `T08--T10`：较远处纹理点，负责检测不合理的全局拖动、漂移或膨胀。

一个好点应当像地图上的固定路口：周围有独特纹理，隔十帧后仍能认出。不要选择：

- 夹爪、夹爪阴影或夹爪反光；
- 会随照明移动的白色高光；
- 纯白、无纹理的组织区域；
- 组织轮廓边缘；
- 两个过于接近、肉眼难以区分的点；
- 明知很快会被夹爪长期遮住的中心像素。

不要因为某个点后来难找就换一个新斑点。原点被挡住时标 `occluded`，离开画面时标
`out_of_view`；重新出现后，再点击同一个组织标志恢复 `visible`。

## 5. 启动标注器

在 ThinLinc 桌面里的终端运行：

```bash
cd /Media_HDD/jwshan/wad/Embodied_gaussians_fixed_super_best
bash scripts/run_super_tissue_gt_annotator.sh
```

这个小工具只使用 OpenCV/X11，不使用 CUDA、OpenGL 或 VirtualGL，因此不受此前 NVIDIA
OpenGL runtime 问题影响。启动器优先使用当前有效的 `$DISPLAY`；如果为空，会在当前用户拥有
的 X socket 中寻找 ThinLinc display，所以不把 display 固定写死成 `:10` 或 `:14`。

如果你明确知道当前会话是 `:14`，也可以运行：

```bash
DISPLAY=:14 bash scripts/run_super_tissue_gt_annotator.sh
```

### 操作键

| 操作 | 按键/鼠标 |
|---|---|
| 第一帧依次放置 10 点 | 左键 |
| 选择 `T01..T09/T10` | `1..9/0` |
| 选择已有标记 | 在标记附近左键一次 |
| 把选中点移动到正确位置或恢复可见 | 再左键点击正确位置 |
| 标成被遮挡 | `O` |
| 标成离开画面 | `X` |
| 将仍有坐标的点恢复为可见 | `V` |
| 确认当前整帧并进入下一帧 | `ENTER` |
| 返回上一采样帧 | `P` |
| 撤销本帧上一步 | `U` |
| 重新计算本帧光流建议 | `R`，仅未确认帧 |
| 保存草稿并退出 | `Q` 或 `ESC` |

右下角会显示当前选中点的放大图。橙色十字表示光流没有可靠找到该点，必须手动移动它，或明确
标成遮挡/离开画面；存在未解决点时，`ENTER` 不会通过。

标注可随时退出并继续，默认文件为：

```text
data/super/evaluation_v1/manual_tissue_tracks_10/annotations.json
```

已确认帧包含 `human_verified=true` 和确认时间；未确认草稿不会被后续评测读取。

## 6. 完成后怎样检查

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/annotate_super_tissue_gt_tracks.py --validate-only
```

必须得到：

```text
passed: true
complete: true
verified_frame_count: 149
scheduled_frame_count: 149
```

如果尚未完成，校验器会返回非零退出码并告诉你缺多少帧。论文结果中还应报告 visible、
occluded、out-of-view 的数量，不能只删除困难帧后报告较漂亮的误差。

## 7. 二维点以后怎样变成三维真值

对每个 `visible` 的左目点 `(u,v)`，后续使用同一时刻通过左右一致性检查的双目深度 `Z`，按
针孔模型反投影：

\[
X_c=\frac{(u-c_x)Z}{f_x},\qquad
Y_c=\frac{(v-c_y)Z}{f_y},\qquad
Z_c=Z.
\]

其中 `(f_x,f_y,c_x,c_y)` 直接读取冻结的 `K_left_rect`，不能重新手调。再用冻结外参把
`(X_c,Y_c,Z_c)` 转到 table/world 坐标。为了避免一个坏视差毁掉三维真值，还要同时保存：

- 左右一致性误差；
- 深度是否有效；
- 深度取样邻域和离点距离；
- 因器械遮挡、时间不同步或无可靠视差而缺失的原因。

无可靠深度时该观测仍可进入 2D 指标，但必须从 3D 分母中排除，不能用前后帧插值假装成
双目真值。三维转换将在 10 点二维标注完成后单独执行，这样人工标点过程不会被深度结果诱导。

## 8. 后续怎样计算估计轨迹误差

初始可见时，把手工点绑定到组织表面三角形的重心坐标或最近的物理节点组合。之后始终传播这
个绑定身份，不能每帧重新挑一个离真值最近的粒子。设第 `i` 个点、第 `t` 帧的人工真值为
`p_{i,t}`，预测为 `p_hat_{i,t}`，仅在人工可见集合 `V` 上计算：

\[
E_{2D}=\frac{1}{|V|}\sum_{(i,t)\in V}
\lVert \hat p^{2D}_{i,t}-p^{2D}_{i,t}\rVert_2,
\]

\[
E_{3D}=\frac{1}{|V_{3D}|}\sum_{(i,t)\in V_{3D}}
\lVert \hat p^{3D}_{i,t}-p^{3D}_{i,t}\rVert_2.
\]

`E_2D` 报像素，`E_3D` 报毫米。同时报告 median、P90/P95、每个动作阶段、每个点和有效观测
覆盖率。这样才看得出平均误差改善是否只是远处静态点太多，或夹取附近的困难点被遮挡分母排除。

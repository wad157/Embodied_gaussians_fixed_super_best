# SUPER 正式 H3 刚度记录

## 1. 当前选择

截至 2026-09-03，SUPER grasp5 的正式在线刚度方案采用已经完整复现的全局 H3
版本：

- Reconstruction 7:1：每个七帧训练块只允许一个互不重叠的完整 H3；
- Future 80:20：训练区间采用已经验证的 available H2--H3 策略，优先使用 H3；
- 只估计全局 distance 倍率和全局速度阻尼；
- shape 固定为 `0.0005`，volume 固定为 `100000`；
- 不启用 `3-of-4 H3`；
- 不启用逐粒子累计局部 distance 场；
- Future 从 frame 1152 起绝对禁止视觉反馈和材料更新。

这里的“采用 H3 刚度”特指输出
`outputs/super_old_h3_reproduction_scale_only_20260903_v1` 对应的冻结配置，不能与
后续的 `3-of-4 + local distance` 消融版本混用。

## 2. H3 目标

对连续三个有监督物理转移 $h=1,2,3$，保存视觉修正前的物理预测
$x_h^{\mathrm{pred}}$。第 $j$ 条 3D 轨迹通过固定表面三角形的重心矩阵
$B_j$ 映射到物理粒子，并以 AllTracker/深度目标 $y_{j,h}$ 监督：

\[
L_{\mathrm{track}}^{(h)}=
\frac{\sum_j c_{j,h}\,
\rho\!\left(B_jx_h^{\mathrm{pred}}-y_{j,h}\right)}
{\sum_j c_{j,h}+\epsilon}.
\]

正式 H3 使用递增权重 $w=(1.5,2,3)$：

\[
L_{H3}(\theta)=
\frac{\sum_{h=1}^{3}w_hL_{\mathrm{track}}^{(h)}}
{\sum_{h=1}^{3}w_h}
+\frac{0.4}{3}\left(\theta_d^2+\theta_\gamma^2\right).
\]

其中：

- \(\theta_d=\log(k_d/k_{d,0})\) 是全局 distance log 倍率；
- \(\theta_\gamma=\log(\gamma/\gamma_0)\) 是速度阻尼 log 倍率；
- $k_{d,0}=0.01$；
- 正式运行的 history 和 graph-smooth 权重均为 0；
- shape 不参与优化，避免 distance/shape 不可辨识。

每个候选都从相同历史状态、相同器械命令和相同接触状态执行连续 XPBD 回放，
不会把三段转移压缩成一个独立单步。

## 3. 梯度与提交

对 distance 和 damping 分别使用中心差分，扰动为
$\delta=0.005$：

\[
g_i^{\mathrm{H3}}=
\frac{L_{H3}(\theta+\delta e_i)-L_{H3}(\theta-\delta e_i)}
{2\delta}.
\]

系统另外执行独立 restarted one-step surrogate，得到
$g^{\mathrm{sur}}$。只有两条梯度方向一致时才允许更新：

\[
\cos(g^{\mathrm{H3}},g^{\mathrm{sur}})\ge 0.95.
\]

Adam 学习率为 `0.03`，候选实际单步 log 变化限制为 `0.02`。提交前还要求：

\[
g^{\mathrm{H3}}\cdot\Delta\theta < 0,
\]

即候选必须是真实 H3 目标的下降方向。Reconstruction 另外要求末尾 H2 与完整
H3 梯度余弦不低于 `0.90`，且累计全局 log 偏移限制为 `0.15`。Future 的累计
限制为 `0.35`；其 H2 只有在两个独立训练块方向余弦不低于 `0.95` 后才可提交，
H2 累计范围限制为 `0.10`。

## 4. 固定运行参数

| 参数 | 数值 |
|---|---:|
| 初始 distance | `0.01` |
| 初始 shape | `0.0005`，固定 |
| volume | `100000`，固定 |
| distance 搜索边界 | `0.00001..4.0` |
| damping 搜索边界 | `2..30 /s` |
| 中心差分扰动 | `0.005` |
| Adam learning rate | `0.03` |
| 单次最大 log step | `0.02` |
| H3 权重 | `1.5, 2, 3` |
| 梯度一致性余弦 | `0.95` |
| 轨迹 robust scale | `0.2 mm` |
| 全局参数 prior weight | `0.4` |
| RGB 位置/速度增益 | `1.0 / 0.15` |
| Gaussian 外观学习 | 关闭 |
| 3-of-4 H3 | 关闭 |
| 局部 distance 场 | 关闭 |

## 5. 完整评估结果

### Reconstruction 7:1

| 方法 | 3D Tracking error (mm) ↓ | 2D Tracking error (px) ↓ | PSNR ↑ | SSIM ↑ | LPIPS ↓ |
|---|---:|---:|---:|---:|---:|
| Pure PBD | 1.546043 | 26.946666 | 22.556952 | 0.778590 | 0.473099 |
| PBD + trajectory + RGB | 0.753540 | 10.504686 | 22.451175 | 0.786105 | 0.473565 |
| **PBD + trajectory + RGB + H3 stiffness** | **0.727802** | **9.117631** | **22.576592** | **0.788351** | **0.472151** |

H3 相对 trajectory+RGB 同时改善五项指标；共提交 6 次，帧为
`335, 343, 383, 551, 591, 1143`。最终全局 distance log 偏移为
`+0.081899`，distance 为 `0.0108535`；最终 damping 为 `11.275 /s`。

### Future 80:20

| 方法 | 3D Tracking error (mm) ↓ | 2D Tracking error (px) ↓ | PSNR ↑ | SSIM ↑ | LPIPS ↓ |
|---|---:|---:|---:|---:|---:|
| Pure PBD | 1.589406 | 26.737669 | 21.882809 | 0.751392 | 0.476906 |
| PBD + trajectory + RGB | 1.530379 | 20.098441 | 21.493695 | 0.744701 | 0.484974 |
| **PBD + trajectory + RGB + H3-based stiffness** | **1.263942** | **15.489142** | **21.737514** | **0.752663** | **0.478689** |

H3-based stiffness 相对 trajectory+RGB 同时改善五项指标；共提交 22 次，提交帧为
`274, 296, 330, 342, 352, 356, 376, 476, 560, 608, 924, 982, 984, 994,`
`1004, 1022, 1024, 1034, 1044, 1054, 1074, 1134`。所有更新均早于 Future
测试边界 1152。最终全局 distance log 偏移为 `+0.032695`，distance 为
`0.0103324`；最终 damping 为 `14.1907 /s`。

## 6. 为什么不采用局部版

`3-of-4 + zero-mean local distance` 完整消融输出为
`outputs/super_h3_three_of_four_local_scale_only_20260903_v1`。它虽然相对
trajectory+RGB 有改善，但 Reconstruction 和 Future 的五项指标都弱于本记录的旧
H3。局部候选只在短窗口内验证，累计后改变了后续全局梯度；Future 最终 distance
被软化到 `0.00813..0.01000`，发生材料漂移。同时被识别出的 `3-of-4` 窗口没有一次
真正提交，因此不能把该消融宣称为有效的 H3 中断恢复。

该实验仅作为反例保留，不是当前正式方法。

## 7. 可复现入口

正式脚本：

```bash
bash scripts/run_super_reproduce_old_h3_scale_only_three_way.sh \
  outputs/<新的输出目录>
```

脚本会显式设置：

```text
PAPER_STIFFNESS_THREE_OF_FOUR_H3=0
PAPER_STIFFNESS_LOCAL_DISTANCE=0
TRAJECTORY_GAUSSIAN_APPEARANCE_ITERATIONS=0
```

已验证结果：

- `outputs/super_old_h3_reproduction_scale_only_20260903_v1/METRICS_COMPACT.md`
- `outputs/super_old_h3_reproduction_scale_only_20260903_v1/METHOD_COMPARISON.json`
- `outputs/super_old_h3_reproduction_scale_only_20260903_v1/RUN_CONFIGURATION.txt`
- `outputs/super_old_h3_reproduction_scale_only_20260903_v1/CODE_AND_INPUT_SHA256.txt`

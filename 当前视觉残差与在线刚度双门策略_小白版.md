# 当前视觉残差与在线刚度“双门”策略：小白也能看懂的版本

本文介绍当前 SUPER 组织仿真的实际运行方法，重点解释三个问题：

1. 视觉 residual 是什么，它为什么可能“当前看着变好了，下一步又弹回去”？
2. 在线刚度是怎么从 residual 中得到候选值的？
3. 为什么现在必须分别设置 residual 保持门和 stiffness 材料隔离门？

本文描述的是 2026-08-21 的当前代码。核心实现位于：

- `examples/example_embodied_super_offline.py`
- `src/embodied_gaussians/physics_simulator/visual_tissue_residual_mapping.py`
- `src/embodied_gaussians/physics_simulator/online_tissue_stiffness.py`

---

## 1. 先用一句话概括现在的方法

当前系统不是看见图像误差就直接改组织，也不是一算出新刚度就立即采用，而是：

> 视觉 residual 先证明自己经过 1、3、5 个无视觉物理步后仍然有用，才允许改粒子位置；刚度候选再关闭 residual，独立证明自己在未来 1、3、5 个视频帧上都比旧刚度预测得更准，才允许改材料参数。

可以把它想象成学生参加两场不同的考试：

- residual 考的是“这次改答案是不是能保持住”，不能刚写完是对的，过一秒就被擦掉；
- stiffness 考的是“材料规律是不是真的更对”，考试时老师不能继续在旁边替它改答案。

整体流程如下：

```text
器械运动 + 当前组织状态
          |
          v
       PBD 物理预测 q_pred
          |
          v
双目图像计算 residual 候选 r，得到 q_corr = q_pred + r
          |
          v
当前帧精确渲染/双目/体积安全门
          |
          v
residual 短保持门：关闭视觉，比较 1/3/5 个物理步
          |
      通过才写回 q_corr
          |
          v
由形变方向和 accepted residual 生成 k_candidate
          |
          v
历史状态稳定门
          |
          v
等待真实未来图像到达
          |
          v
stiffness 材料隔离门：关闭 residual，比较 H=1/3/5 个视频帧
          |
          v
      通过只写回刚度 k，不写回影子粒子状态
```

---

## 2. 最容易混淆的几个名词

| 符号或名词 | 小白解释 |
|---|---|
| $q_{rest}$ | 组织没有变形时，每个物理粒子的初始位置 |
| $q_{pred}$ | 只依靠 PBD、器械和接触算出的当前位置，即视觉修正前的物理预测 |
| $r$ | 视觉认为粒子还应该移动多少，也叫 visual residual |
| $q_{corr}=q_{pred}+r$ | 加上视觉修正后的候选位置 |
| $q_d$ | 粒子速度。当前 residual 只改位置 $q$，不直接给速度 $q_d$ 加冲量 |
| $k_{verified}$ | 已经通过验证、当前真正使用的刚度 |
| $k_{candidate}$ | 根据新视觉证据提出、但还没有通过考试的候选刚度 |
| $D_{vis}$ | 渲染结果和真实双目图像之间的视觉误差，越小越好 |
| $J$ | 四面体当前体积与初始体积的比例；$J\leq0$ 表示翻转，是严重错误 |
| shadow rollout | 复制一份仿真状态，在副本里试验，不污染正在播放的 live 状态 |
| 开放环 | 试验过程中不再用视觉 residual 把结果拉回图像 |

### 两种 H 完全不是一回事

这是全文最重要的区分：

| 门 | `H=1/3/5` 的单位 | 用哪张图像 | 目的 |
|---|---|---|---|
| residual 短保持门 | **物理步** | 始终使用当前图像 $I_t$ | 检查这次位置修正会不会马上被物理弹回 |
| stiffness 材料隔离门 | **未来视频帧** | 使用真实的 $I_{t+1},I_{t+3},I_{t+5}$ | 检查候选刚度有没有更好的未来预测能力 |

例如一次视频帧通常会执行 3 个物理步。因此“未来 5 个视频帧”和“未来 5 个物理步”不是同一个时间长度。

---

## 3. 为什么需要视觉 residual

PBD 负责根据材料、接触和器械运动预测组织：

$$
q_{pred}=\operatorname{PBDStep}(q_t,q_{d,t},u_t,k)
$$

其中 $u_t$ 表示当前器械位姿、夹爪角度、direct anchors 和 support nodes 等控制量，$k$ 表示材料刚度。

现实中的组织参数不可能一开始就完全准确，所以只靠物理可能与真实视频有偏差。视觉 residual 负责估计一个小的增量 $r$：

$$
q_{corr}=q_{pred}+r
$$

这里的含义不是“视觉替代物理”，而是：

> 物理先给出大方向，视觉只做有上限的小修正。

当前 GUI 默认单次 residual 上限为 `0.10 mm`；当前正式复核命令使用 `0.20 mm`。这只是每次观测的节点增量上限，不代表所有粒子都会移动这么多。

---

## 4. residual 到底在优化什么

### 4.1 从粒子位置得到图像

每个组织 Gaussian 都绑定到物理组织表面。粒子移动后，Gaussian 也跟随移动。简化地写为：

$$
\mu_g(q)=\sum_i w_{gi}q_i
$$

$\mu_g$ 是第 $g$ 个 Gaussian 的中心，$w_{gi}$ 是它对周围物理粒子的绑定权重。把这些 Gaussian 渲染出来，就得到预测图像：

$$
\hat I_c=\operatorname{Render}_c(q)
$$

$c$ 表示左相机或右相机。

### 4.2 双目视觉误差

对每个相机分别计算经过 mask 加权的图像误差：

$$
D_c(q)=
\frac{\sum_p w_{c,p}\,\rho\!\left(\hat I_{c,p}(q)-I_{c,p}\right)}
{\max\left(\sum_p w_{c,p},1\right)}
$$

其中：

- $p$ 是像素；
- $w_{c,p}$ 是组织 mask、器械遮挡和边界等共同形成的像素权重；
- $\rho$ 是 Smooth-L1 鲁棒误差，少数特别亮、特别暗或错误像素不会无限放大影响。

左右相机先各自归一化，再等权平均：

$$
D_{vis}(q)=\frac{1}{|\mathcal C|}\sum_{c\in\mathcal C}D_c(q)
$$

这样不会因为某一只相机有效像素更多，就完全压过另一只相机。

### 4.3 总优化目标

residual 不是只追求图像相似。否则它可能为了少量像素，把几个粒子拉飞或把四面体翻过来。实际优化目标是：

$$
\begin{aligned}
L(r)=
&\lambda_{vis}D_{vis}(q_{pred}+r)\\
&+\lambda_dL_d
+\lambda_vL_v
+\lambda_sL_s\\
&+\lambda_{sp}L_{sp}
+\lambda_tL_t
+\lambda_mL_m
\end{aligned}
$$

当前权重为：

| 项 | 当前权重 | 直观作用 |
|---|---:|---|
| $D_{vis}$ | `1.0` | 让渲染接近真实图像 |
| $L_d$ | `0.4` | 不让相邻粒子距离被本次修正突然拉坏 |
| $L_v$ | `1.0` | 不让本次修正造成不合理体积变化 |
| $L_s$ | `0.008` | 不让四面体形状被本次修正过度剪切 |
| $L_{sp}$ | `0.05` | 邻居 residual 尽量平滑，减少孤立粒子飞走 |
| $L_t$ | `0.10` | 限制本次增量与时间参考差异过大 |
| $L_m$ | `0.01` | residual 本身尽量小 |

几个正则项可以写成：

$$
L_d=\frac{1}{|E|}\sum_{(i,j)\in E}
\left(
\frac{\|q_i^{corr}-q_j^{corr}\|-\|q_i^{pred}-q_j^{pred}\|}
{\ell_{ij}^{rest}}
\right)^2
$$

$$
L_v=\frac{1}{|T|}\sum_{\tau\in T}
\left(J_\tau(q_{corr})-J_\tau(q_{pred})\right)^2
$$

$$
L_{sp}=\frac{1}{|E|r_{max}^2}
\sum_{(i,j)\in E}\|r_i-r_j\|^2
$$

$$
L_m=\frac{1}{Nr_{max}^2}\sum_i\|r_i\|^2
$$

当前实现还有一个非常重要的细节：$L_d$、$L_v$ 和 $L_s$ 都以**视觉修正前的当前物理状态 $q_{pred}$** 为参考，而不是强行以 $q_{rest}$ 为参考。

也就是说，器械真实压弯组织后，零 residual 的几何正则严格为零。视觉模块不会每看一帧就额外把组织往初始平面拉一次。

---

## 5. residual 以前为什么会起反作用

### 问题一：旧时间参考可能重复注入已经执行过的位移

residual 是相对当前 $q_{pred}$ 的增量。如果上一帧的 residual 已经写入了粒子位置，下一帧又把它当作新 residual 的起点，就可能重复移动：

```text
上一帧已经向右移动 0.1 mm
下一帧又从“向右 0.1 mm”开始优化
即使图像没有新证据，也可能继续向右漂
```

现在每次 residual 都从零增量开始：

$$
r^{(0)}=0
$$

`previous_residual_carry` 默认是 `0`，上一帧结果最多只作为可配置的正则参考，不会默认重复写入。

### 问题二：旧几何正则把真实接触形变当成错误

如果组织已被夹爪压弯，却仍将每次 residual 与 $q_{rest}$ 比较，视觉优化会同时受到“回到初始形状”的拉力。现在几何正则改为相对 $q_{pred}$，只限制本次视觉增量，不重复承担材料恢复工作。

### 问题三：只看当前一瞬间，无法判断修正是否能保持

旧门只检查：

$$
D_{vis}(q_{corr},I_t)<D_{vis}(q_{pred},I_t)
$$

但 residual 只改位置 $q$，不直接修改速度 $q_d$。因此可能出现：

1. 刚写回时图像变好了；
2. 原来的速度和 PBD 约束仍指向另一个方向；
3. 下一物理步马上弹回、过冲或引起局部跳动。

“当前帧更好”不等于“这是一个能被物理保持的好状态”。

### 本次改动：新增 residual 短开放环保持门

当前先复制两份完全相同的仿真状态：

- baseline：不写入 residual；
- candidate：写入 $q_{corr}=q_{pred}+r$。

两条分支都：

- 关闭 visual residual；
- 不计算 visual force；
- 保持相同器械位姿；
- 冻结当前 persistent grip 状态，不允许一条分支新抓住而另一条没抓住；
- 分别运行 1、3、5 个物理步。

在每个物理步 horizon 上，用同一张当前图像 $I_t$ 计算：

$$
D_{base}^{res}(h)=D_{vis}(q_{base}^{(h)},I_t)
$$

$$
D_{corr}^{res}(h)=D_{vis}(q_{corr}^{(h)},I_t)
$$

定义保持改善量：

$$
\Delta_{res}(h)=D_{base}^{res}(h)-D_{corr}^{res}(h)
$$

只有：

$$
\Delta_{res}(1)>0,
\quad\Delta_{res}(3)>0,
\quad\Delta_{res}(5)>0
$$

三项全部成立，才允许写回最初的 $q_{corr}$。

注意：写回的是“刚做完 residual 的状态”，不是影子分支运行 5 步后的状态。那 5 步只是考试，不能偷偷算进真实时间。

如果 H3 反弹，即使 H1 和 H5 都更好，也会拒绝并恢复原来的 live 状态。日志会出现类似：

```text
open_loop_persistence_not_improved:H3
```

---

## 6. 为什么抓取附近不让 residual 随便改

抓取激活时，direct anchors 和 support nodes 已经由器械明确控制。如果视觉 residual 同时去拉这些粒子，就会形成两个控制器互相拔河：

```text
夹爪说：“这个粒子跟着尖端走。”
视觉说：“这个粒子应该往图像另一边走。”
结果：局部粒子跳动、拉扯或飞散。
```

现在 residual 和刚度学习都会排除：

1. direct anchors；
2. support nodes；
3. 与它们相邻的**严格一层**四面体邻域。

“严格一层”表示只从原始抓取节点扩张一次，新加入的邻居不会继续向外扩张。因此它保护抓取核心，但不会把大半个组织都排除在视觉监督之外。

固定粒子、局部体积不安全粒子也不会参与相应更新。

---

## 7. 在线刚度候选是怎样产生的

### 7.1 先判断物理是“太软”还是“太硬”

对第 $i$ 个粒子，定义物理形变：

$$
d_i=q_{pred,i}-q_{rest,i}
$$

视觉接受的修正是 $r_i$。用它们的夹角判断方向：

$$
c_i=-\frac{d_i\cdot r_i}
{\max(\|d_i\|\,\|r_i\|,\epsilon)}
$$

为什么前面有负号？

- 如果物理把粒子拉得太远，而视觉要求它往初始位置退回来，那么 $d_i$ 与 $r_i$ 方向相反，$d_i\cdot r_i<0$，所以 $c_i>0$，倾向于**变硬**；
- 如果视觉还要求粒子沿当前形变方向继续走，那么 $d_i$ 与 $r_i$ 同向，$c_i<0$，倾向于**变软**。

加入当前 `0.15` 的轻微变硬偏置，并乘上 residual 与形变强度：

$$
s_i=
\operatorname{clip}(c_i+0.15,-1,1)
\cdot
\min\left(\frac{\|r_i\|}{0.20\text{ mm}},1\right)
\cdot
\min\left(\frac{\|d_i\|}{0.75\text{ mm}},1\right)
$$

同时要求：

$$
\|r_i\|\geq0.02\text{ mm},
\qquad
\|d_i\|\geq0.10\text{ mm}
$$

太小的 residual 或形变很可能只是渲染噪声，不用于更新刚度。

### 7.2 哪些粒子有资格提供刚度证据

一个粒子必须同时满足：

- 不是固定边界粒子；
- residual 前后所有相邻四面体都有 $J\geq0.03$；
- 双目图像对该粒子有非忽略的视觉梯度；
- 不属于 active direct/support 及其严格一环排除区；
- 当前不是捕获/释放、严重穿透或接触面突然切换时刻。

普通稳定接触并不会自动关闭刚度学习。当前快速 q7 门为关闭状态；真正暂停的是捕获/释放变化、穿透超过 `2.8 mm` 或明显接触 patch 跳变，并且只冷却一次更新。

### 7.3 空间平滑和时间 EMA

原始信号先进行 1 次邻域平滑，混合比例为 `0.35`，避免一个孤立粒子把局部刚度改得特别尖锐。

再做时间指数平均：

$$
e_{i,t}=0.70e_{i,t-1}+0.30s_{i,t}
$$

它的意思是：旧证据占 70%，新证据占 30%。这样不会因为一帧噪声突然大改材料，又能在几次有效观测后看见变化。

候选等待未来 H5 图像时，候选的刚度值会冻结，保证考试公平；但 EMA 不再冻结。后续每一帧通过门禁的
2D 图像 residual 仍继续更新 $e_{i,t}$。因此系统同时保留两份东西：

- `pending candidate`：已经进入考试的旧快照，绝不在考试中途换题；
- `live evidence EMA`：继续吸收新图像，为下一位 candidate 做准备。

如果 pending candidate 通过，只提交它的刚度，不把 live EMA 倒回旧快照；如果失败，只把产生该候选的
局部 source 节点乘以 `0.85`，其他区域在等待期间新积累的证据仍然保留。以前只要一次 residual 被拒绝就
全局清空 EMA，相当于把几帧有效观察也一起扔掉，现在已经取消这种全局清空。

### 7.4 在 log 空间更新

先计算本次 log 步长：

$$
\delta_i=\operatorname{clip}(0.18e_{i,t},-0.18,0.18)
$$

先生成基础 log 方向，但它现在只是“建议方向”，不是唯一答案：

$$
k_{dist,i}^{cand}
=\operatorname{clip}
\left(k_{dist,i}^{verified}e^{\delta_i},0.10,2.00\right)
$$

$$
k_{shape,i}^{cand}
=\operatorname{clip}
\left(k_{shape,i}^{verified}e^{1.5\delta_i},0.003,0.020\right)
$$

log 空间的好处是软化和硬化更对称。例如 $e^{+0.1}$ 与 $e^{-0.1}$ 是互为倒数的比例变化，而不是简单加减一个固定数。

当前 volume stiffness 固定为 `1e10`，在线只更新 distance 和 shape。初始值为：

| 参数 | 初始值 | 在线范围 |
|---|---:|---:|
| distance stiffness | `0.20` | `0.10 ... 2.00` |
| volume stiffness | `1e10` | 固定，不在线更新 |
| shape stiffness | `0.004` | `0.003 ... 0.020` |

此时只产生 $k_{candidate}$，不会立刻覆盖 $k_{verified}$。

### 7.5 distance 与 shape 独立、正负双向搜索

视觉启发式可能把“太硬/太软”的符号猜错，也可能只需要改拉伸刚度而不需要改形状刚度。因此当前不会只沿
$\delta_i$ 的正方向同时修改两个参数，而是在严格关闭 residual 的材料影子中测试：

| candidate | distance log 步长 | shape log 步长 |
|---|---:|---:|
| distance forward / reverse | $+2\delta_i$ / $-2\delta_i$ | $0$ |
| shape forward / reverse | $0$ | $+2(1.5\delta_i)$ / $-2(1.5\delta_i)$ |
| joint forward / reverse | $+\delta_i$ / $-\delta_i$ | $+1.5\delta_i$ / $-1.5\delta_i$ |
| joint large forward / reverse | $+2\delta_i$ / $-2\delta_i$ | $+2(1.5\delta_i)$ / $-2(1.5\delta_i)$ |

每一项最终仍受单次 `maximum_log_step=0.18` 和绝对刚度上下界约束。这样即使启发式方向猜反，reverse
candidate 仍能被 H1/H3/H5 考试发现；如果图像只支持 distance 或只支持 shape，另一个参数可以保持不变。

### 7.6 刚度场本身的 graph smoothness

只平滑 residual 信号还不够：连续多次更新后，某一个粒子的刚度仍可能变成孤立尖峰。现在对每个候选的
**刚度场本身**增加局部图平滑。令 $z_i=\log k_i$，对应的图能量是：

$$
L_{graph}(k)=\frac{1}{|E_{local}|}
\sum_{(i,j)\in E_{local}}(z_i-z_j)^2
$$

默认在 candidate source 附近做 2 次局部更新，每次混合 `0.20`：

$$
z_i\leftarrow(1-0.20)z_i+0.20\frac{1}{|\mathcal N(i)|}
\sum_{j\in\mathcal N(i)}z_j
$$

它只在允许学习的局部粒子图中传播；固定粒子、抓取排除区和质量不合格区会精确恢复原 verified 值。
因此它会压低“一个点特别硬、旁边突然特别软”的数值尖峰，但不会跨过硬 mask 污染抓取核心。确定性测试中，
一个单点刚度突变的 distance log-graph energy 从 `0.0108` 降到 `0.000506`，约降低 `95.3%`。

这和 7.3 节的 signal smoothing 是两件事：前者平滑“证据”，这里平滑“候选材料场”。GUI 中也分别暴露为
`Spatial smoothing ...` 和 `Stiffness graph smoothing ...`。

### 7.7 实时输入仍然只有 2D 图像

在线刚度证据来自左右 RGB 图像计算出的 2D 渲染 residual。运行时不读取点云、深度图、人工 3D 轨迹或
3D 真值。仿真器内部当然仍有 3D 粒子位置，否则无法执行 PBD；但这些是系统自己的物理状态，不是额外的
3D 传感器观测。报告中的 3D tracking error 只用于离线实验评分，绝不会反馈给在线更新器。

---

## 8. 刚度以前为什么“看起来在优化，实际作用不清楚”

### 旧问题一：主验证仍然重放 residual

之前 baseline 和 candidate 影子分支里还会继续求 residual。于是比较的其实是：

$$
\text{旧刚度}+\text{视觉不断纠错}
\quad\text{vs}\quad
\text{新刚度}+\text{视觉不断纠错}
$$

如果 candidate 的物理预测很差，但 residual 在后面把它重新拉回图像，最终 loss 仍可能很好。此时记录到的是“物理加老师补答案”的效果，不是刚度本身的效果。

### 旧问题二：两条分支的抓取状态可能不同

如果一条影子分支在 rollout 中新捕获了组织，而另一条没有，两者的结果差异主要来自抓取状态机，不是材料刚度。

### 旧问题三：只看一段闭环平均值会掩盖单点退化

例如：

| horizon | baseline loss | candidate loss | candidate 是否更好 |
|---:|---:|---:|---|
| H1 | `0.0200` | `0.0197` | 是 |
| H3 | `0.0205` | `0.0207` | **否** |
| H5 | `0.0210` | `0.0206` | 是 |

三点平均可能仍然更好，但 H3 已经明确反弹。旧的累计均值可能放过这种候选。

### 旧问题四：把影子粒子状态一起写回 live

之前通过验证后，除了刚度，还可能把 H5 影子分支的粒子状态写回 live。主门改为关闭 residual 后，这相当于把“无视觉实验分支”灌进“有视觉实时分支”，会破坏两者隔离。

---

## 9. 本次改成怎样的 stiffness 材料隔离考试

### 9.1 两条分支只允许刚度不同

在 residual 已通过并生成候选的时刻 $t$，保存完整状态 $S_t$。建立两条分支：

$$
\text{baseline}: S_t+k_{verified}
$$

$$
\text{candidate}: S_t+k_{candidate}
$$

两条分支必须使用完全相同的：

- 初始粒子位置 $q$ 和速度 $q_d$；
- 后续器械 SE(3) 与夹爪 $q_7$ 命令；
- 接触参数和 top barrier；
- 当前已经存在的 direct anchors、support nodes 和目标；
- 相机、图像及 mask。

并且严格关闭：

- visual residual；
- visual force；
- rollout 内新的在线刚度更新；
- 新的抓取捕获/释放状态转换。

这就像两辆车使用同一条路、同一位司机、同一初始速度，只替换悬挂弹簧，然后比较谁更稳。

### 9.2 等真实未来图像再评分

系统收集到未来第 5 个视频帧后，从保存的 $S_t$ 重放，并在 H1、H3、H5 的真实图像上分别计算：

$$
D_{old}(h)=D_{vis}(\operatorname{Render}(q_{old,t+h}),I_{t+h})
$$

$$
D_{cand}(h)=D_{vis}(\operatorname{Render}(q_{cand,t+h}),I_{t+h})
$$

定义刚度预测改善量：

$$
\Delta_k(h)=D_{old}(h)-D_{cand}(h)
$$

当前要求三个点逐个成立：

$$
\Delta_k(1)>0,
\quad\Delta_k(3)>0,
\quad\Delta_k(5)>0
$$

还要求三点平均改善超过噪声 margin：

$$
\frac{1}{3}\sum_{h\in\{1,3,5\}}\Delta_k(h)
\geq10^{-5}
$$

所以前面示例中 H3 变差的候选会被拒绝，即使平均值略好也不行。

### 9.3 不只是图像好看，还要物理安全

候选还必须同时满足：

1. 双目平均 loss 不能出现超出容差的单相机退化；
2. 不能增加翻转四面体；
3. 最小 $J$ 不能比 baseline 明显下降；
4. 最大穿透不能恶化超过 `0.10 mm`；
5. grip anchor RMS 和最大误差不能恶化超过 `0.05 mm`；
6. 历史准静态状态不能明显更难保持；
7. 刚度值和单次 log 步长不能越界。

最小体积比的容许下界为：

$$
J_{cand}^{min}\geq
\max\left(J_{old}^{min}-0.01,\ 0.98J_{old}^{min}\right)
$$

单相机容许上界为：

$$
D_{cand,c}\leq D_{old,c}
+\max\left(10^{-6},0.005D_{old,c}\right)
$$

### 9.4 历史状态门

系统最多保存 4 个过去的准静态 accepted 状态。对旧刚度和候选刚度分别做一次固定接触/抓取的短松弛，计算质量加权 RMS：

$$
L_{hist}(k)=
\frac{1}{|\mathcal H|}
\sum_{h\in\mathcal H}
\sqrt{
\frac{\sum_i m_i\|q_{i,h}^{relaxed}(k)-q_{i,h}^{accepted}\|^2}
{\sum_i m_i}}
$$

要求：

$$
L_{hist}(k_{cand})
\leq1.10L_{hist}(k_{verified})+0.01\text{ mm}
$$

它防止候选只对当前几帧有利，却让过去已经稳定对齐的状态全部塌掉。

### 9.5 候选搜索不再被启发式符号和参数耦合限制

系统对 7.5 节的 8 个候选逐一运行相同的 H1/H3/H5 材料隔离考试。在所有 horizon、双目、体积、穿透、
anchor 和历史门都通过的版本中，选择平均视觉 loss 最低者。若全部失败，就保留旧刚度；只把失败候选原
source 区域的 EMA 保留比例降为 `0.85`，不删除 pending 期间其他区域的新证据。

### 9.6 最终到底写回什么

通过时只做：

$$
k_{verified}\leftarrow k_{candidate}
$$

不会写回：

- baseline 影子粒子状态；
- candidate 的 H5 影子粒子状态；
- 影子 rollout 中的临时抓取状态；
- 影子 rollout 中的临时时间进度。

因此材料考试不会让用户看到组织突然跳到某个影子分支的位置。

---

## 10. 改进前后对照

| 项目 | 改进前 | 当前策略 |
|---|---|---|
| residual 当前帧检查 | 检查即时视觉改善和体积安全 | 保留 |
| residual 物理保持 | 没有，可能下一步弹回 | 新增无视觉 1/3/5 物理步保持门 |
| residual 时间增量 | 可能重复携带已执行位移 | 默认 carry=`0`，每次从零增量开始 |
| residual 几何参考 | 可能把真实接触形变拉回 rest | 只约束相对当前 $q_{pred}$ 的增量 |
| 抓取核心视觉修改 | 容易与 anchors 拉扯 | 排除 direct/support 及严格一环 |
| stiffness 主验证中的 residual | 开启，会替材料纠错 | **严格关闭** |
| stiffness 主验证抓取状态机 | 可继续变化 | 冻结现有绑定，禁止新捕获/释放 |
| stiffness 评分 horizon | 闭环 `1..H` 平均可能掩盖反弹 | 精确 H1/H3/H5，三个点必须分别改善 |
| stiffness 写回 | 可能连 H5 影子粒子状态一起采用 | 只提交刚度参数 |
| pending 期间材料证据 | 等待候选时丢掉后续帧 | candidate 冻结考试，live EMA 继续积累 |
| candidate 方向 | 只沿启发式正方向 | 正向和反向都由无 residual 影子实测 |
| distance / shape | 同倍率耦合修改 | distance-only、shape-only、joint 独立比较 |
| 刚度空间连续性 | 只平滑 residual 信号 | 额外最小化局部 $L_{graph}(k)$，抑制刚度孤点 |
| 在线观测 | 容易误解为需要 3D 数据 | 只用双目 2D RGB residual；无点云/深度/3D 真值反馈 |
| 结果可解释性 | 很难区分 residual、抓取和刚度贡献 | 主门测材料本身；end-to-end 仅作辅助评估 |

---

## 11. 当前完整运行时序

每次实时运行大致执行下面的顺序：

1. 读取当前视频时间戳的器械 SE(3) 和夹爪 $q_7$。
2. 运行 PBD 物理步；每个物理步内部当前有 12 个 substeps。
3. 处理普通夹爪接触、top barrier、persistent grip、distance/volume/shape 约束。
4. 得到视觉修正前的 $q_{pred}$，由组织表面更新 Gaussian。
5. 默认每 3 个物理步求一次双目 residual；同一视频图像不会重复学习。
6. 优化 $r$，先做当前帧精确渲染、双目和四面体安全检查。
7. 若即时检查通过，运行 residual baseline/candidate 的 1/3/5 无视觉物理步保持门。
8. 保持门通过才把最初的 $q_{corr}$ 写回 live；失败则恢复 $q_{pred}$。
9. 只有 accepted residual 才能提供刚度证据。
10. 对有效区域计算方向信号、邻域平滑、EMA，冻结一份基础 candidate 快照。
11. 先过历史准静态稳定门；失败立即丢弃。
12. 候选等待真实未来第 5 个视频帧；不创建第二个 candidate，但 accepted 2D residual 继续更新 live EMA。
13. 用保存的状态分别跑旧刚度和候选刚度的 material-isolation H1/H3/H5。
14. 对 distance/shape 独立的正负 8 个候选逐一检查视觉、体积、接触和历史门；每个候选先做局部刚度图平滑。
15. 选择最好的安全候选，只提交 distance/shape stiffness；否则保留旧刚度。
16. 对已提交候选继续记录 H1/H3/H5/H10 的材料隔离和 end-to-end 辅助评估。

对于 future 80/20 正式评测，后 20% 禁止视觉观测时会取消尚未完成的候选；不能偷偷使用测试帧帮助刚度提交。

## 12. 怎样阅读 GUI 和日志

### residual 保持门

GUI 的 `Visual residual diagnostics` 会显示：

```text
No-vision physics hold H1/H3/H5 improvements
```

控制台会显示：

```text
hold_H1/3/5=+.../+.../+...
```

解释方法：

- 三个数都大于 0：保持门通过；
- 任意一个小于或等于 0：该 residual 回滚；
- `NA`：即时门已经拒绝，未运行保持门，或者 horizon 数据缺失。

### stiffness 状态

常见状态：

| 状态或拒绝原因 | 意思 |
|---|---|
| `candidate` | 已产生候选，尚未验证 |
| `pending_horizon` | 正在等待未来第 5 个视频帧 |
| `committed` | H1/H3/H5 及安全门通过，刚度已提交 |
| `no_active_material_signal` | 没有同时满足方向、幅值、图像监督和 mask 的节点 |
| `material_isolation_horizon_not_improved:H3` | 候选在 H3 没有比旧刚度好 |
| `material_isolation_horizons_incomplete` | 没拿到完整的 H1/H3/H5 精确帧证据 |
| `prediction_gap_not_improved` | 三点平均改善没有超过 `1e-5` |
| `camera_regression` | 某只相机退化超过容差 |
| `volume_quality_regression` | 最小 $J$ 或翻转情况恶化 |
| `penetration_regression` | 器械穿透恶化 |
| `grip_anchor_regression` | 抓取 anchor 误差恶化 |
| `history_regression` | 过去准静态状态变得更难保持 |

正式 stiffness 输出的 `metadata.json` 应明确包含：

```text
stiffness_admission_horizons: [1, 3, 5]
stiffness_admission_visual_residual_enabled: false
stiffness_admission_grip_state_machine_frozen: true
stiffness_adopt_validated_rollout: false
visual_residual_persistence_horizons_physics_steps: [1, 3, 5]
stiffness_online_observation_source: stereo_2d_rgb_residual_only
stiffness_online_uses_point_cloud: false
stiffness_online_uses_observed_3d_points: false
stiffness_evidence_accumulates_while_pending: true
```

这些字段可以防止以后拿错实验目录，把旧闭环门结果误当成新材料隔离结果。

---

## 13. 一个真实 residual 保持门例子

真实 CUDA SUPER frame `350..357` 冒烟中，一次 residual 的当前视觉 loss 从：

$$
0.016553\rightarrow0.016065
$$

然后短保持门得到：

| 物理步 horizon | baseline loss | corrected loss | $\Delta_{res}$ |
|---:|---:|---:|---:|
| H1 | `0.016553` | `0.016132` | `+0.000421` |
| H3 | `0.016553` | `0.016307` | `+0.000247` |
| H5 | `0.016553` | `0.016433` | `+0.000120` |

三个改善量都大于 0，所以允许写回。

这个例子还能看出为什么必须检查多个物理步：改善从 H1 的 `0.000421` 降到 H5 的 `0.000120`，说明物理确实在逐步消解一部分视觉修正。它最终仍为正，所以这次修正“虽然会衰减，但没有反过来变坏”。

三次真实 residual update 的完整保持改善分别为：

```text
[4.210e-4, 2.468e-4, 1.204e-4]
[3.286e-4, 1.899e-4, 7.657e-5]
[3.326e-4, 1.705e-4, 8.110e-5]
```

它们都通过。包含两条 5 步影子分支和 6 次额外渲染后，每次 residual update 在该冒烟中的耗时为 `0.297 ... 0.413 s`，平均 `0.343 s`。

这只能证明新门能运行且这三次 residual 能保持，不能代替完整轨迹 A/B 结论。

---

## 14. 这次更改解决了什么，又没有保证什么

### 已经解决的逻辑问题

- 不再用 residual 的持续纠错冒充刚度收益；
- 不再用不同抓取状态冒充材料差异；
- 不再因为平均值更好就放过某个 horizon 明确反弹的刚度候选；
- 不再把 residual-off 的 H5 影子粒子状态写入 residual-on live 状态；
- 不再接受只在当前瞬间好看、几步后就变坏的 residual；
- 日志可以明确追溯每个 horizon 为什么通过或拒绝。

### 仍然不能直接保证的事情

- 视觉 mask、Gaussian 颜色或相机标定错误时，$D_{vis}$ 仍可能不是完美老师；
- 通过门只表示“在当前可见证据和短 horizon 上更好”，不等于恢复了真实材料参数；
- H1/H3/H5 都好，不保证 H10 或更长未来一定好，所以提交后仍保留 H10 辅助评估；
- 新 residual 门增加了计算量；
- 完整 reconstruction、future 80/20、重复性和渲染指标仍要以新逻辑的正式评测结果为准。

当前双向解耦 + graph-smoothness 新逻辑的正式评测目录为：

```text
outputs/super_tissue_evaluation_residual_online_bidirectional_graph_20260821_v2/
```

只有目录中出现 `COMPLETE`，并且 `status.txt` 两组状态都为 0，才能把它当成本次策略的完整实验结果。

---

## 15. 最后用最简单的话再说一遍

以前的问题是：

> 视觉刚把组织拉得更像图像，就马上相信它；刚度考试时视觉还在旁边帮忙，抓取状态也可能变化，最后甚至把考试副本的粒子位置带回真实运行。因此很难知道到底是 residual 有效、刚度有效，还是两者互相遮掩。

现在改成：

> residual 必须先证明自己不会被接下来 1/3/5 个物理步立即推翻；刚度必须在完全关闭 residual、冻结抓取状态的条件下，独立赢过旧刚度的未来 H1/H3/H5。通过后 residual 只写自己的当前位置修正，刚度只写自己的材料参数，两个模块各自负责、各自考试。

这就是当前“双门、双隔离、只写回自己负责内容”的核心策略。
